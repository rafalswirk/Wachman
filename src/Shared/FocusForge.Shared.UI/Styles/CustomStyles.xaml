<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
    xmlns:converters="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf">

    <!-- Neon Color Resource -->
    <Color x:Key="NeonOrangeColor">#FFFFA500</Color>
    <Brush x:Key="NeonOrangeBrush">#FFFFA500</Brush>
    <Brush x:Key="DarkNeonOrangeBrush">#FFFD6B3F</Brush>
    <Color x:Key="NeonCyanColor">#FF00FFFF</Color>
    <Color x:Key="NeonPinkColor">#FFFF00FF</Color>

    <Style x:Key="MenuToggle" TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="Gray"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Content">
            <Setter.Value>
                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Top" >
                    <wpf:PackIcon Kind="Alarm" VerticalAlignment="Center" VerticalContentAlignment="Center" Foreground="Gray" HorizontalContentAlignment="Left"/>
                    <TextBlock Text="123" />
                </WrapPanel>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Foreground" Value="{StaticResource NeonOrangeBrush}"/>
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="False">
                <Setter Property="Foreground" Value="Gray"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource DarkNeonOrangeBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Neon Glow Effect -->
    <DropShadowEffect x:Key="NeonGlowEffect"
                      Color="{StaticResource NeonOrangeColor}"
                      BlurRadius="25"
                      ShadowDepth="0"
                      Opacity="1"/>

    <!-- Neon TextBlock Style -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource NeonOrangeBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Effect" Value="{StaticResource NeonGlowEffect}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
    </Style>

    <Style x:Key="NeonButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource NeonOrangeBrush}"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource NeonOrangeBrush}"/>
        <Setter Property="BorderThickness" Value="1.5"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Margin" Value="5,2"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource NeonOrangeBrush}"/>
    </Style>

    <Style x:Key="ActivityViewContainerBorderStyle" TargetType="Border">
        <Setter Property="Margin" Value="6"/>
        <Setter Property="Padding" Value="12 10"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Background" Value="#12000000"/>
        <Setter Property="BorderBrush" Value="#88FFA500"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#FFFFA500" BlurRadius="10" ShadowDepth="0" Opacity="0.55"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ActivityViewCardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#00001600"/>
        <Setter Property="BorderBrush" Value="#30FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="10 8"/>
        <Setter Property="Margin" Value="0 0 0 10"/>
    </Style>

    <Style x:Key="ActivityViewTitleTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#FFF2F2F2"/>
        <Setter Property="Effect" Value="{x:Null}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="ActivityViewValueTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#FFF5F5F5"/>
        <Setter Property="Effect" Value="{x:Null}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="ActivityViewSecondaryTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#FFD6D6D6"/>
        <Setter Property="Effect" Value="{x:Null}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <Style x:Key="ActivityViewDataGridStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#FFEFEFEF"/>
        <Setter Property="BorderBrush" Value="#88FFA500"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowHeight" Value="36"/>
        <Setter Property="ColumnHeaderHeight" Value="40"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#30FFFFFF"/>
        <Setter Property="VerticalGridLinesBrush" Value="#30FFFFFF"/>
        <Setter Property="RowBackground" Value="#12000000"/>
        <Setter Property="AlternatingRowBackground" Value="#18000000"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow">
                    <Setter Property="Foreground" Value="#FFEFEFEF"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#FFEFEFEF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ActivityViewDataGridHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#22000000"/>
        <Setter Property="Foreground" Value="#FFF2F2F2"/>
        <Setter Property="BorderBrush" Value="#55FFA500"/>
        <Setter Property="BorderThickness" Value="0 0 1 1"/>
        <Setter Property="Padding" Value="10 8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="ActivityViewDataGridCellStyle" TargetType="DataGridCell">
        <Setter Property="Foreground" Value="#FFEFEFEF"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#30FFFFFF"/>
        <Setter Property="BorderThickness" Value="0 0 1 1"/>
        <Setter Property="Padding" Value="8 0 6 0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="TextBlock.TextAlignment" Value="Left"/>
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Grid Background="{TemplateBinding Background}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>-->
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#FFEFEFEF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ActivityViewTreeStyle" TargetType="TreeView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#88FFA500"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
    </Style>

    <Style x:Key="ActivityViewTreeItemStyle" TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="#FFEFEFEF"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

</ResourceDictionary>