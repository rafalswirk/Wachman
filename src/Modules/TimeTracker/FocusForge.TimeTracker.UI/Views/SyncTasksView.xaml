<UserControl x:Class="FocusForge.TimeTracker.UI.Views.SyncTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FocusForge.TimeTracker.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Content="Load tasks" Command="{Binding LoadTasksCommand}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <Button Content="Import selected" Command="{Binding ImportSelected}" HorizontalAlignment="Left" Margin="113,10,0,0" VerticalAlignment="Top"/>
        <ListView Grid.Row="1" MaxHeight="300"  d:ItemsSource="{d:SampleData ItemCount=50}" ItemsSource="{Binding Tasks}" Margin="0,17,0,10" Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <CheckBox IsChecked="{Binding Import}" Content="{Binding Task.Name}" Foreground="WhiteSmoke" Margin="5" Background="Orange"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
