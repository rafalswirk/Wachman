<UserControl x:Class="FocusForge.TimeTracker.UI.Views.SyncTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FocusForge.TimeTracker.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Load tasks" Command="{Binding LoadTasksCommand}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Center" Height="32" Width="98"/>
        <Button Grid.Row="0" Content="Import selected" Command="{Binding ImportSelected}" HorizontalAlignment="Left" Margin="113,10,0,0" VerticalAlignment="Center" Height="32" Width="132"/>
        <Expander Header="Tasks" Grid.Row="1" Margin="5 10" Foreground="Orange" IsExpanded="{Binding IsExpanded}" Background="Transparent" BorderThickness="1" BorderBrush="AntiqueWhite">
            <ItemsControl d:ItemsSource="{d:SampleData ItemCount=50}" ItemsSource="{Binding Tasks}" Background="Transparent">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding Import}" Content="{Binding Task.Name}" Foreground="WhiteSmoke" Margin="5" Background="Orange"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Expander>
    </Grid>
</UserControl>
